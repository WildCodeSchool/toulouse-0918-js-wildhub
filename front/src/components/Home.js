import React, { Component } from 'react';
import LoginPage from './LoginPage';

class Home extends Component {
    render() {
        return (
            <div id='home-page'>
                <LoginPage />
            </div>
        );
    }
}

export default Home;
